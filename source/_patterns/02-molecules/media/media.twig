{% set classes = [
  'jcc-media',
  media.classes_other ? media.classes_other : '',
]|merge(banner.classes|default([''])) %}

<figure role="group" class="{{ classes|join(' ')|trim }}">
  {{ media.entity }}
  <figcaption class="jcc-media__caption">{{ media.caption }}</figcaption>
</figure>